import news
import const

def test_const():
	assert(len(const.CATEGORIES) > 0)
	assert(len(const.COUNTRIES) > 0)
	assert(len(const.LANGUAGES) > 0)
	assert(len(const.LIVE_SOURCES) > 0)
	assert(len(const.ORDERINGS) > 0)

	
